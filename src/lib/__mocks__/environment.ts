/**
 * Mock for $app/environment in vitest
 *
 * This mock provides static values for testing purposes.
 * These values simulate a non-browser, development, non-building environment.
 * Only use in test contexts - not for production code.
 */
export const browser = false;
export const dev = true;
export const building = false;

